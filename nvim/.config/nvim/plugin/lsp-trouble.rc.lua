-- local status, trouble = pcall(require, "trouble")
-- if not status then
--   print "Trouble is not installed"
--   return
-- end

-- trouble.setup {
--   auto_preview = false,
--   auto_fold = true,
-- }
